<h1 id="tickets-title">Tickets</h1>
<div id="tickets">
  <% @tickets.each do |ticket| %>
    <div>
      <h5><%= link_to ticket.title, ticket_path(ticket.id), :class => "ticket-link"%></h5>
    </div>
  <% end %>
  <%= link_to "Back", root_path, :class => "back-button" %>
</div>