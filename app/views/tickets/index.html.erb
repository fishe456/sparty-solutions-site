<h1 id="tickets-title">Tickets</h1>
<div id="tickets">
  <% @tickets.each do |ticket| %>
    <div class="ticket-display">
        <%= link_to ticket.title, ticket_path(ticket.id) %>
    </div>
  <% end %>
</div>